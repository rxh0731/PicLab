# 图片实验室独立版开发约定

## 项目范围

本项目是独立的 PySide6 图片预处理软件，仅修改 `D:\code\piclab`。`D:\Code\FontMgr` 及其他字库编辑器目录只能作为只读历史参考，未经用户明确要求不得修改。

## 开发原则

- 所有用户可见内容、代码注释、日志、进度信息和文档使用简体中文。
- 函数名、变量名和属性名使用英文，保持现有 Python 风格。
- 原稿文件始终只读；人工操作和算法参数保存到 `.fontlab` SQLite 项目。
- 图片像素不存入项目数据库，完整图片继续保存在用户选择的文件路径中。
- 画布使用持久 `QImage`、`QPainter` 和局部刷新，避免交互期间反复进行整图格式转换。
- 预览、文字区域检测、高清区域加载和完整尺寸导出等耗时任务必须放入后台线程。
- 保留导出停止、临时文件和原子替换机制，失败不得留下不完整目标文件。
- 不自动打包便携版，不提交或推送，除非用户明确要求。

## 目录职责

- `core/`：背景清理、笔画重建、区域检测算法。
- `data/`：图片实验室项目 SQLite 存储和日志。
- `services/`：图片载入、预览、高清区域处理和完整尺寸导出。
- `ui/`：独立窗口、页面、画布、主题和后台任务。
- `tests/`：图片实验室专项测试。
- `assets/`：独立软件图标等资源。

## 新会话开始步骤

1. 完整阅读 `AGENTS.md`、`README.md`、`开发说明.md` 和 `图片实验室_算法研究记录.md`。
2. 查看当前文件清单、Git 状态和未提交变化，保留用户已有工作。
3. 使用项目自己的 `.venv\Scripts\python.exe` 运行和测试。
4. 修改前先分析相关算法、服务和界面调用链；修改后运行专项测试和编译检查。

## 验证命令

```text
.venv\Scripts\python.exe -m unittest discover -s tests -p "test_image_*.py"
.venv\Scripts\python.exe -m compileall -q core data services ui
```

