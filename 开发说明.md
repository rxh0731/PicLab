# 图片实验室独立开发说明

本目录是从字库编辑器中拆出的独立软件，不包含字库、排版、导入、审核、优化和导出字库等功能。新的开发对话只应修改 `D:\code\piclab`，不得回写 `D:\Code\FontMgr`。

## 当前入口

`main.py` 创建 `ImageLabWindow`，中央页面为 `ui.pages.image_lab_page.ImageLabPage`。页面右上角“退出”按钮用于关闭当前窗口。

## 数据流

打开图片 -> `ImageLabService.create_project` -> 后台生成预览 -> `ImageLabCanvas` 显示原稿、清理效果、清理层和区域 -> 保存为 `.fontlab` SQLite 项目 -> 按需导出清理结果、白色清理层或 PSD/PSB。

## 可扩展边界

- 算法改动放在 `core/image_cleanup.py`、`core/algorithms.py` 或 `core/image_regions.py`。
- 项目字段和迁移放在 `data/image_lab_project_store.py`。
- 文件处理和导出放在 `services/image_lab_service.py`。
- 画布交互放在 `ui/widgets/image_lab_canvas.py`，页面控件和后台任务协调放在 `ui/pages/image_lab_page.py`。
- 所有耗时处理必须通过 `ui/workers.py` 放入线程池，不能阻塞 Qt 主线程。

## 验证

在 `.venv` 中运行：

```text
python -m unittest discover -s tests -p "test_image_*.py"
python -m compileall -q core data services ui
```
