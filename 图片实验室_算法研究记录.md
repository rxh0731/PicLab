# 图片实验室算法研究记录

## 1. 记录目的

本文件记录图片实验室在真实样本上的处理方法、判断依据和参数含义。研究结果必须落实为程序内可选择、可保存、可重复执行的能力，不能依赖开发人员针对单张图片临时修改代码。

## 2. Image00139.tif 样本

- 样本路径：`D:\字库样本\测试图片\Image00139.tif`
- 图片类型：RGB TIFF，LZW 压缩
- 图片尺寸：7867 × 17965 像素
- 分辨率：300 DPI
- 文件大小：约 280 MB
- 版面特征：亮底深色文字，文字密集；背景包含大量细颗粒、纵向细纹、拓印破损和边缘黑区。

样本只用于只读研究，不复制到项目，不写回原文件。

## 3. 原通用算法的问题

原“多通道通用识别”同时保护深色、浅色和彩色局部结构，适合颜色未知的普通文献，但不适合这张高密度拓片。纸纹、破损和文字在局部窗口内都形成强烈明暗变化，算法因此把大部分底纹也识别为前景。

在长边 2200 像素的缩略预览中，将清理强度从 35 提高到 80，保留前景仍约为 98%，结果变化很小。由此确认问题不是强度不足，而是处理路线选择错误。

## 4. 试验路线

### 4.1 全局阈值和局部背景阈值

全局 Otsu 和局部背景残差 Otsu 可以去除灰色底色，但仍会保留大量与文字同样深的细颗粒。它们适合作为候选前景，不能单独作为最终结果。

### 4.2 普通形态学开运算

三像素开运算能删除部分孤立点，但也会同步侵蚀残损细笔。只增加开运算半径会导致笔画断裂，不宜直接作为用户的强度控制。

### 4.3 笔画尺度核心重建

最终采用以下步骤：

1. 判断页面是否属于中性亮底、深色字迹；彩色文献和反白文字继续使用通用多通道算法。
2. 使用形态学闭运算估计文字下方的局部亮背景。
3. 计算“局部背景亮度 - 当前像素亮度”，得到只针对深色笔迹的对比度图。
4. 使用 Otsu 取得强前景基础阈值，清理强度只在基础阈值附近调整，不写死某张图片的绝对灰度。
5. 对强前景执行距离变换，只把具有一定宽度的区域作为笔画核心。单像素颗粒和细纸纹通常不能形成核心。
6. 只在较弱前景范围内、有限距离内从核心向外生长，恢复笔画边缘和局部残损；禁止沿连续纸纹扩散到整页。
7. 按当前估计笔画尺度删除缺乏面积和跨度的孤立域。
8. 输出白色透明清理层，原稿始终不变；证据不足的删除区域进入红色待核对视图。

原尺寸 2048 × 2048 中部区域的试验结果如下：

| 档位 | 保留前景占比 | 观察 |
|---|---:|---|
| 保守 | 约 27.6% | 浅色和残损笔画风险最低，底纹残留较多 |
| 均衡 | 约 23.2% | 主笔画完整，底纹明显减少 |
| 强力 | 约 20.4% | 斑点进一步减少，需要重点核对残损细笔 |
| 很强 | 约 18.8% | 适合背景污染严重区域，不建议未经核对直接整图采用 |

当前程序用连续的“清理强度”表达上述变化，并由图片尺寸自动换算笔画核心半径、边缘恢复距离和孤立域门槛。

## 5. 程序内使用方法

处理方式提供三种选择：

- `自动识别（推荐）`：中性亮底深色字自动使用拓片算法；彩色、反白或无法稳定判断的图片使用通用算法。
- `通用彩色文献`：适合颜色未知、彩色笔迹、泛黄纸张或浅色文字。
- `拓片深色文字`：明确按亮底深字和笔画尺度处理，适合本次样本。

建议处理 `Image00139.tif` 时先选择“自动识别”或“拓片深色文字”，从强度 50 开始，保留“清除孤立小噪点”。需要优先保护残损笔画时勾选“保护浅色和残损笔迹”；背景仍多时逐步提高强度，并在“待核对区域”视图检查被删除的细笔。

算法无法可靠判断历史破损究竟是纸纹还是原始笔画。用户应先保存 `.fontlab` 项目，再使用“保护文字”恢复误删区域，最后导出包含锁定原稿和白色清理层的 PSD/PSB 继续精修。

## 6. 可重复性

图片实验室项目保存以下内容：

- 原稿路径、尺寸、修改时间和文件大小；
- 用户选择的处理方式和全部处理参数；
- 当前图片清理算法版本；
- 最近一次预览实际采用的识别方式；
- 人工清除背景和保护文字笔触。

当前算法版本为 6。完整尺寸导出复用预览阶段的背景校准和实际处理方式，避免每个分块重新判断后产生不一致结果。

## 7. 后续研究重点

- 增加局部高清预览，避免整图缩略预览掩盖细笔损失。
- 研究字符布局引导，在不依赖文字识别结果的前提下进一步剔除字符间的大块破损。
- 建立包含干净参考蒙版的真实样本集，分别统计笔画召回率、背景去除率和人工修复时间。
- 学习模型只能作为可选候选，必须通过原稿对照和字形保真核对，不能直接生成或重绘历史笔画。

## 8. Image00112.tif 不均匀背景

- 样本路径：`D:\字库样本\测试图片\Image00112.tif`
- 图片尺寸：7685 × 17185 像素。
- 版面特征：左侧、上部和边缘污染明显重于右下区域，同一全局清理强度无法兼顾各区域。

算法版本 2 在清理强度 37 时使用全图唯一 Otsu 阈值；不同区域的平均亮度约为 157～208，深色像素占比约为 12.5%～30.2%，因此重污染纹理会被统一门槛当作文字。简单逐块 Otsu 会让每块把自身污染重新解释成正常分布，并产生预览、高清区域和最终导出不一致，不能采用。

算法版本 3 改为以下稳定流程：

1. 在整图缩略分析层计算一次基础强笔迹阈值。
2. 按局部暗点密度、缺乏稳定笔画核心的纹理密度和局部对比度建立低分辨率污染难度图，并进行平滑处理。
3. 重污染区提高强笔迹阈值和笔画核心尺度，并限制弱边缘恢复距离；较干净区域保持基础门槛。
4. 参数图使用整幅原图坐标。快速预览、放大后的高清区域和完整尺寸重叠分块读取同一位置的参数，分块内不再重新 Otsu。
5. 笔画核心尺度按整幅原图在当前处理分辨率下换算，不再按单个导出块的短边决定。

在长边 2200 像素预览、清理强度 37、保护浅色笔迹和清除孤立噪点的条件下，基础强阈值约为 89.9，局部阈值范围约为 89.9～132.0；保留前景占比由版本 2 的约 53.6% 降到约 40.0%，完全清理占比由约 46.4% 提高到约 60.0%。这说明程序已针对污染较重区域自动加强清理，但与正文同样深、同样粗且连成片的破损仍无法仅凭像素可靠区分，必须结合红色待核对区域和“保护文字”工具复核。

## 9. 强度为零的保真规则

此前强度为 0 仍会执行拓片阈值、距离变换、种子重建、孤立域过滤和页面边界遮罩，因此即使滑块归零也可能丢失浅色或残损笔画。算法版本 4 起将强度 0 定义为“原稿保真（未自动清理）”：清理层完全透明、合成预览逐像素等于原稿、页面边界不参与裁切，且不产生待核对误删区域。算法版本 5 进一步规定强度 1～25% 使用保真下限，额外保留尚有歧义的暗纹和细笔，避免重污染区域的核心判定吞掉清晰笔画。用户仍可通过人工“清除背景”笔画增加白色覆盖；“保护文字”笔画仍可用于恢复已有覆盖。

## 10. Image00112.tif 四边四角专用清理

`Image00112.tif` 在清理强度 34 时，中心区域已经能够清除一部分底纹，但四边和四角仍残留成片杂点。原因不是单一全局阈值偏低：满幅拓片的压边、漏墨和扫描阴影会贴着原图边界形成大面积或长距离连通区域，并通过笔画核心、弱边缘恢复以及低强度保底保护重新进入前景。单纯提高整图强度会同步抬高中心区域门槛，造成正文细笔画丢失。

算法版本 6 增加独立的边缘污染自适应流程：

1. 所有形态学背景估计使用反射边界，避免图像外部的默认填充值影响四边背景。
2. 按整幅原图坐标计算连续边缘风险带；四角自然叠加风险，离开边缘后平滑归零，不产生硬分界。
3. 背景越不均匀，边缘区域的强笔迹阈值和笔画核心尺度提高得越多，同时收紧弱证据支持和恢复距离；中心区域继续使用原有局部污染参数。
4. 低强度保底保护在中心继续保留残损细笔，但边缘区域需要更强证据才能恢复，防止把整片漏墨重新保留。
5. 边缘风险固定到原图坐标。高清预览或完整导出的内部处理分块不会把自身边界误认为原图边界，只有真实的整图四边参与专用处理。
6. 处理指标增加“边缘污染自适应”“边缘风险均值”和“边缘阈值增量最大值”，用于核对当前图片是否启用了边缘专用清理。

在长边 1600 像素的整图分析层、清理强度 34 下，`Image00112.tif` 的边缘高置信前景占比由约 55.8% 降至约 39.3%，中心约为 38.0%；强度 32、34、36 的保留前景占比分别约为 69.6%、64.5%、60.3%，变化连续。该统计说明边缘不再明显比中心残留更多，同时没有通过提高整图门槛来换取边缘清理。真实历史笔画若贴近边框且与压边污染同色、同粗、同连通关系，仍不存在完全可靠的纯像素判定，必须结合原稿和“待核对区域”复核。

## 11. 文字区域框线和网格定位

密集竖排文献中，文字行检测器返回的各列起止位置和宽度会有数像素抖动。旧实现按每一列自己的长度独立等分字符，误差会沿整列累计，相邻列的框线也不会对齐，容易出现框线穿过字形或偏向相邻字符的问题。

文字区域检测现改为以下流程：

1. 从全部文字行共同估计列间节距，去除重复行后把实际列和补全列吸附到同一版面网格。
2. 沿字符排列方向统计整页前景投影，在列间节距附近联合搜索字符节距和低墨迹边界相位。
3. 每一列复用相同的字符边界，仅用局部前景占比判断空白格是否跳过，不再按各列起止点分别等分。
4. 区域轮廓使用不随画布缩放变化的固定一像素线；选中、确认、拒绝和已处理状态继续通过颜色、虚线及控制点区分。

该方法不识别文字内容，也不改变原稿或清理蒙版。版面存在明显错行、跨栏标题或异形批注时，用户仍可使用“手工补框”、顶点调整、合并和拆分工具复核。
